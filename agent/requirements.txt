# Full pinned freeze of the shared agent/venv (the whole Python service:
# sourcing, matching, tailoring, tracking, review_gate), for exact
# reproducibility (e.g. the planned Docker build). Direct dependencies,
# grouped by which module needs them, are declared in pyproject.toml —
# this file is the complete resolved set, direct + transitive, and can't
# be cleanly attributed per module once transitive deps overlap.
#
# Entries below trace back to matching/, ollama==0.6.2 for tailoring/'s
# llm/client.py, and playwright==1.62.0 plus pypdf==6.14.2 for tailoring/'s
# write/write.py (Chromium print-to-PDF, then a page-count check on the
# rendered PDF to detect one-page overflow) — re-freeze this file as other
# modules add their own deps.
#
# pytest/pytest-playwright/pytest-base-url are present because they're
# installed in venv (test-writing is the user's own domain, not something
# this freeze curates around) — not a pyproject.toml dependency yet.
#
# torch is a CPU-only build (+cpu). If a fresh install can't resolve it from
# PyPI alone, add: --extra-index-url https://download.pytorch.org/whl/cpu

annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.2
certifi==2026.7.22
charset-normalizer==3.4.9
click==8.4.2
cuda-bindings==13.3.1
cuda-pathfinder==1.6.0
cuda-toolkit==13.0.3.0
defusedxml==0.7.1
filelock==3.32.0
fsspec==2026.6.0
greenlet==3.5.4
h11==0.16.0
hf-xet==1.5.2
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.24.0
idna==3.18
iniconfig==2.3.0
Jinja2==3.1.6
joblib==1.5.3
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
narwhals==2.24.0
networkx==3.6.1
nltk==3.10.0
numpy==2.5.1
ollama==0.6.2
packaging==26.2
playwright==1.62.0
pluggy==1.6.0
pydantic==2.13.4
pydantic_core==2.46.4
pyee==13.0.1
Pygments==2.20.0
pypdf==6.14.2
pytest==9.1.1
pytest-base-url==2.1.0
pytest-playwright==0.8.0
python-slugify==8.0.4
PyYAML==6.0.3
regex==2026.7.19
requests==2.34.2
rich==15.0.0
safetensors==0.8.0
scikit-learn==1.9.0
scipy==1.18.0
sentence-transformers==5.6.0
setuptools==83.0.0
shellingham==1.5.4
sympy==1.14.0
text-unidecode==1.3
threadpoolctl==3.6.0
tokenizers==0.22.2
torch==2.13.0+cpu
tqdm==4.69.0
transformers==5.14.1
typer==0.27.0
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
